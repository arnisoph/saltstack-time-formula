{% load_yaml as rawmap %}
Debian:
  general: {}
  ntpd:
    service:
      name: ntp
    pkgs:
      - ntp
  ntpdate:
    pkgs:
      - ntpdate
RedHat:
  general: {}
  ntpd:
    service:
      name: ntpd
    pkgs:
      - ntp
  ntpdate:
    pkgs:
      - ntpdate
{% endload %}
